# .env.example — copy to .env.local for dev, set in Vercel dashboard for prod

# Backend URL
# Dev: leave empty to use Vite proxy (/api → localhost:3000)
# Prod: set to your Render backend URL
VITE_API_URL=https://your-backend.onrender.com/api
